before_script:
  - printenv
  - echo
  - npm install

run-test:
  script:
    - npm run lint
    - npm test
    - npm run test-upgrade

# TODO: Need SLS on builder
#aws-integration-test:
#  script:
#    - creds-helper -aws
#    - npm run test-integration
