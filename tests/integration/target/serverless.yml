service: target

provider:
  name: aws
  runtime: nodejs8.10

functions:
  root:
    handler: handler.handler
    events:
      - http: ANY /

  proxy:
    handler: handler.handler
    events:
      - http: ANY /{proxy+}
