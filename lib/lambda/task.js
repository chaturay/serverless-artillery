const def = require('./taskDef')
const exec = require('./taskExec')
const plan = require('./taskPlan')
const result = require('./taskResult')
const valid = require('./taskValid')

module.exports = {
  def,
  exec,
  plan,
  result,
  valid,
}
